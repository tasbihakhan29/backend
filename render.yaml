services:
  - type: web
    name: medsync-backend
    runtime: java
    plan: free
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/medsync-backend-1.0.0.jar
    healthCheckPath: /api/public/health

